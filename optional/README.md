# 可选迁移工具

本目录不属于日常插件开发流程。只有需要从旧系统迁移元数据、业务功能或业务数据时才使用。

- `migration/legacy-export/`：旧系统元数据导出和分页业务数据查询工具、配置模板及输出说明。
- `migration/samples/`：实体、功能、场景、动作及插件改写的迁移样板。

使用前先阅读 `../docs/migration/`。如果只是新建或维护插件，可以忽略整个 `optional/` 目录。
